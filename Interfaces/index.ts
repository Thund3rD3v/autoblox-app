export interface IBloxburgCashier {
  topLeft: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  bottomRight: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  top: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  oni: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  che: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  bef: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  vef: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  tom: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  let: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  bottom: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  burger: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  sides: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  drinks: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  item1: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  item2: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  item3: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  size1: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  size2: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  size3: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
  done: {
    status: "unset" | "setting" | "set";
    position: { x: number; y: number };
  };
}
